# ⚠️ SECURITY WARNING: Never commit this file with actual values!
# Copy this file to .env and fill in your actual credentials

# Server Configuration
PORT=5000
HOST=0.0.0.0

# Database Configuration
# Get this from MongoDB Atlas: https://cloud.mongodb.com
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname

# JWT Configuration
# Generate a strong random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_minimum_32_characters

# Email Configuration (for low stock alerts)
# For Gmail: Use App Password (requires 2FA): https://myaccount.google.com/apppasswords
EMAIL_SERVICE=gmail
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_16_character_app_password
EMAIL_FROM=your_email@gmail.com

# Environment (development/production)
NODE_ENV=development

# CORS (Optional - for production, comma-separated allowed origins)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
